export declare class DiscordTime {
  /**
   * The time to display.
   */
  timestamp: number;
  /**
   * The format for the time.
   */
  format: 't' | 'T' | 'f' | 'F' | 'd' | 'D' | 'R';
  private time;
  private updateInterval;
  render(): any;
  connectedCallback(): void;
  disconnectedCallback(): void;
  /**
   * Generates a string for the time.
   */
  private update;
}
