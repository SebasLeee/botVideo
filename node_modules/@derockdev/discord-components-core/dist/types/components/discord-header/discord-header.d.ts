import { ComponentInterface } from '../../stencil-public-runtime';
export declare class DiscordHeader implements ComponentInterface {
  /**
   * The guild name
   */
  guild: string;
  /**
   * The name of the channel
   */
  channel: string;
  /**
   * The icon to display.
   */
  icon?: string;
  render(): any;
}
